import { InfoPopupDirective } from './info-popup.directive';

describe('InfoPopupDirective', () => {
  it('should create an instance', () => {
    const directive = new InfoPopupDirective();
    expect(directive).toBeTruthy();
  });
});
