import { VideoPopupDirective } from './video-popup.directive';

describe('VideoPopupDirective', () => {
  it('should create an instance', () => {
    const directive = new VideoPopupDirective();
    expect(directive).toBeTruthy();
  });
});
