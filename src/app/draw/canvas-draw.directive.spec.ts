import { CanvasDrawDirective } from './canvas-draw.directive';

describe('CanvasDrawDirective', () => {
  it('should create an instance', () => {
    const directive = new CanvasDrawDirective();
    expect(directive).toBeTruthy();
  });
});
